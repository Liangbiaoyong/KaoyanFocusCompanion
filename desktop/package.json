{
  "name": "kaoyan-focus-companion-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "考研专注养成 · Windows 桌宠",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . KaoyanFocusCompanion --platform=win32 --arch=x64 --out=dist --overwrite --no-asar --extra-resource=../src --ignore=\"(node_modules|test|scripts|dist|\\.npmrc|package-lock\\.json)\""
  },
  "devDependencies": {
    "@electron/packager": "^20.3.0",
    "electron": "^44.3.0"
  }
}
